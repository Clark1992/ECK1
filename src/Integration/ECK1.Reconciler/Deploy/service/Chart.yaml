apiVersion: v2
name: eck1-reconciler
description: Service chart for ECK1 Reconciler
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: config-watcher
    version: 0.1.0
    repository: "oci://localhost:5000/helm"
