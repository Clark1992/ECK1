master:
  replicas: 1
  persistence:
    storageClass: "hostpath"

data:
  replicas: 1
  persistence:
    storageClass: "hostpath"

toolbox:
  image: localhost:5000/alpine-toolbox:dev

resources:
  requests:
    cpu: 200m
    memory: 1Gi
  limits:
    cpu: 500m
    memory: 1Gi
